# 📂 โครงสร้างโปรเจค Fei Yi Transporter

## 🗂️ โครงสร้างไฟล์ทั้งหมด

```
📦 Fei-Yi-Transporter/
│
├── 📄 index.html                    ← หน้าจองคิวสำหรับลูกค้า
├── 📄 dashboard.html                ← หน้า Dashboard หลังบ้าน
├── 📄 manage-trips.html             ← หน้าจัดการรอบรถ
├── 📄 bookings.html                 ← หน้าดูรายการจอง
│
├── 📁 css/
│   ├── 🎨 style.css                 ← CSS สำหรับหน้าลูกค้า
│   └── 🎨 admin-style.css           ← CSS สำหรับหลังบ้าน
│
├── 📁 js/
│   ├── 🔥 firebase.js               ← Config Firebase
│   ├── ⚙️ app.js                    ← Logic จองคิว (หน้าลูกค้า)
│   ├── ⚙️ dashboard.js              ← Logic Dashboard
│   ├── ⚙️ manage-trips.js           ← Logic จัดการรอบรถ
│   └── ⚙️ bookings.js               ← Logic รายการจอง
│
├── 📖 README.md                     ← คู่มือการใช้งานทั่วไป
└── 📖 ADMIN_GUIDE.md                ← คู่มือหลังบ้าน
```

---

## 📄 รายละเอียดไฟล์แต่ละไฟล์

### 🟢 หน้าลูกค้า (Frontend)

#### **index.html**
- หน้าจองคิวสำหรับลูกค้า
- แสดงรอบรถที่เปิดใช้งาน
- ฟอร์มจองคิว
- ใช้: `css/style.css` + `js/app.js` + `js/firebase.js`

#### **css/style.css**
- CSS สำหรับหน้าลูกค้า
- Gradient background
- Responsive design
- Cards และ Forms

#### **js/app.js**
- ดึงรอบรถจาก Firebase (เฉพาะที่ active: true)
- จองคิว → บันทึกลง `bookings` collection
- อัพเดทที่นั่ง → ลดจำนวน `seats` ใน `trips`
- Import จาก: `firebase.js`

---

### 🔴 หลังบ้าน (Admin Backend)

#### **dashboard.html**
- Dashboard หลักของหลังบ้าน
- Sidebar Navigation (เมนูด้านซ้าย)
- แสดงสถิติ 4 ตัว:
  - จำนวนรอบรถทั้งหมด
  - รอบรถที่เปิดใช้งาน
  - จำนวนการจองวันนี้
  - ที่นั่งที่จองแล้ว
- ภาพรวมรอบรถ + สถานะ
- การจอง 5 รายการล่าสุด
- ใช้: `css/admin-style.css` + `js/dashboard.js`

#### **manage-trips.html**
- หน้าจัดการรอบรถ
- **เพิ่มรอบรถใหม่** (Form ด้านบน)
- **แสดงรายการรอบรถ** (Cards ด้านล่าง)
- **แก้ไขรอบรถ** (Modal popup)
- **ลบรอบรถ** (ปุ่มลบ + ยืนยัน)
- ใช้: `css/admin-style.css` + `js/manage-trips.js`

#### **bookings.html**
- หน้าดูรายการจองทั้งหมด
- **ตัวกรอง**: รอบรถ, วันที่, เบอร์โทร
- **ตาราง**: แสดงการจองทั้งหมด
- **Modal รายละเอียด**: คลิก "ดู" เพื่อดูข้อมูลครบ
- ใช้: `css/admin-style.css` + `js/bookings.js`

#### **css/admin-style.css**
- CSS สำหรับหลังบ้าน
- Sidebar Navigation
- Stats Cards
- Trip Cards
- Data Tables
- Modals
- Responsive

#### **js/dashboard.js**
- โหลดสถิติจาก Firebase:
  - นับรอบรถ (`trips` collection)
  - นับการจองวันนี้ (`bookings`)
- แสดงภาพรวมรอบรถ
- แสดงการจอง 5 รายการล่าสุด
- Import จาก: `firebase.js`

#### **js/manage-trips.js**
- **เพิ่มรอบรถ** → `addDoc()` ไปที่ `trips`
- **โหลดรายการ** → `getDocs()` จาก `trips`
- **แก้ไข** → `updateDoc()` ใน `trips`
- **ลบ** → `deleteDoc()` จาก `trips`
- Import จาก: `firebase.js`

#### **js/bookings.js**
- **โหลดรายการจอง** → `getDocs()` จาก `bookings`
- **กรอง**: ตามรอบรถ, วันที่, เบอร์โทร
- **แสดงรายละเอียด** → Modal popup
- Import จาก: `firebase.js`

---

### 🔧 Core Files

#### **js/firebase.js**
- Config Firebase
- Export: `app`, `db`, `auth`, `analytics`
- ใช้โดยไฟล์ JS ทั้งหมด

---

### 📖 Documentation

#### **README.md**
- คู่มือการติดตั้งและใช้งานทั่วไป
- โครงสร้าง Database
- Firebase Free Tier
- วิธีใช้งาน

#### **ADMIN_GUIDE.md**
- คู่มือการใช้งานหลังบ้าน
- ฟีเจอร์ Admin Panel
- Tips & Best Practices
- Security Recommendations

---

## 🔗 ความสัมพันธ์ระหว่างไฟล์

### 🟢 หน้าลูกค้า
```
index.html
    ↓ load
├── css/style.css
└── js/app.js
        ↓ import
    js/firebase.js
```

### 🔴 Dashboard
```
dashboard.html
    ↓ load
├── css/admin-style.css
└── js/dashboard.js
        ↓ import
    js/firebase.js
```

### 🔴 จัดการรอบรถ
```
manage-trips.html
    ↓ load
├── css/admin-style.css
└── js/manage-trips.js
        ↓ import
    js/firebase.js
```

### 🔴 รายการจอง
```
bookings.html
    ↓ load
├── css/admin-style.css
└── js/bookings.js
        ↓ import
    js/firebase.js
```

---

## 📊 Firebase Collections

### **Collection: trips**
```javascript
{
  route: "กรุงเทพ → อุดรธานี",
  time: "08:00",
  seats: 10,                    // ลดลงเมื่อมีการจอง
  price: 500,
  active: true,                  // true = แสดงในหน้าลูกค้า
  createdAt: "2025-12-16..."
}
```

**ใช้โดย:**
- ✅ `app.js` - อ่านเพื่อแสดงรอบรถ
- ✅ `dashboard.js` - อ่านเพื่อแสดงสถิติ
- ✅ `manage-trips.js` - CRUD (สร้าง/อ่าน/แก้ไข/ลบ)
- ✅ `bookings.js` - อ่านเพื่อกรอง

### **Collection: bookings**
```javascript
{
  tripId: "abc123",             // อ้างอิงไปที่ trips
  route: "กรุงเทพ → อุดรธานี",
  time: "08:00",
  customerName: "สมชาย",
  customerPhone: "0812345678",
  seats: 2,                     // จำนวนที่นั่งที่จอง
  totalPrice: 1000,
  bookingDate: "2025-12-16...",
  status: "confirmed"
}
```

**ใช้โดย:**
- ✅ `app.js` - สร้างการจองใหม่
- ✅ `dashboard.js` - อ่านเพื่อแสดงสถิติ
- ✅ `bookings.js` - อ่านเพื่อแสดงรายการ

---

## 🚀 เส้นทางการใช้งาน

### 🔄 Flow: ลูกค้าจองคิว
1. ลูกค้าเปิด `index.html`
2. `app.js` โหลดรอบรถจาก Firebase (active: true)
3. ลูกค้ากรอกข้อมูล + เลือกรอบรถ
4. `app.js` บันทึกการจองลง `bookings`
5. `app.js` ลด `seats` ของรอบรถที่เลือก
6. แสดงหมายเลขการจอง

### 🔄 Flow: แอดมินเพิ่มรอบรถ
1. แอดมินเปิด `manage-trips.html`
2. กรอกข้อมูล: เส้นทาง, เวลา, ที่นั่ง, ราคา
3. `manage-trips.js` บันทึกลง `trips` collection
4. รอบรถปรากฏใน `index.html` ทันที

### 🔄 Flow: แอดมินดูรายการจอง
1. แอดมินเปิด `bookings.html`
2. `bookings.js` ดึงข้อมูลจาก `bookings`
3. แสดงในรูปแบบตาราง
4. สามารถกรอง/ค้นหาได้

---

## 💡 ข้อควรรู้

### 🟢 สำหรับผู้ใช้งาน (Customer)
- เปิดแค่ `index.html` เท่านั้น
- ไม่มีสิทธิ์เข้าหลังบ้าน

### 🔴 สำหรับแอดมิน
- เปิด `dashboard.html` เป็นหน้าแรก
- สามารถเข้าถึงทุกหน้าผ่าน Sidebar
- สามารถจัดการรอบรถและดูการจองทั้งหมด

### 🔧 สำหรับนักพัฒนา
- ไฟล์ทั้งหมด import จาก `firebase.js`
- ใช้ Firebase Firestore เป็น Database
- ES6 Modules (import/export)
- No framework - Pure JavaScript

---

## 📱 Responsive Breakpoints

```css
Desktop:    > 768px   → Sidebar เต็ม
Tablet:     768px     → Sidebar แสดงไอคอน
Mobile:     < 480px   → Sidebar ซ่อน
```

---

✅ **โครงสร้างชัดเจน แยกหน้าที่ชัดเจน**
🎯 **ใช้งานง่าย แก้ไขสะดวก**
📱 **Responsive รองรับทุกอุปกรณ์**
