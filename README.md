# 🚐 Fei Yi Transporter - ระบบจองคิวรถออนไลน์

ระบบจองคิวรถออนไลน์แบบ Real-time ที่ใช้ Firebase Firestore

## ✨ ฟีเจอร์

### สำหรับผู้ใช้งาน (index.html)
- ✅ เลือกรอบรถที่มี (แสดงเฉพาะรอบที่เปิดใช้งาน)
- ✅ กรอกข้อมูลผู้จอง (ชื่อ, เบอร์, จำนวนที่นั่ง)
- ✅ ระบบตรวจสอบที่นั่งว่าง
- ✅ แสดงหมายเลขการจอง
- ✅ อัพเดทที่นั่งอัตโนมัติ

### สำหรับแอดมิน (admin.html)
- ✅ เพิ่มรอบรถใหม่
- ✅ กำหนดเส้นทาง, เวลา, ที่นั่ง, ราคา
- ✅ เปิด/ปิดการใช้งานรอบรถ
- ✅ ดูรายการรอบรถทั้งหมด

## 📂 โครงสร้างไฟล์

```
project/
├── css/
│   └── style.css          # ไฟล์ CSS หลัก
├── js/
│   ├── firebase.js        # การตั้งค่า Firebase
│   ├── app.js             # Logic หน้าจองคิว
│   └── admin.js           # Logic หน้าแอดมิน
├── index.html             # หน้าจองคิว (ผู้ใช้งาน)
├── admin.html             # หน้าเพิ่มรอบรถ (แอดมิน)
└── README.md              # ไฟล์นี้
```

## 🔧 การติดตั้ง

### 1. สร้าง Firebase Project
1. ไปที่ https://console.firebase.google.com/
2. สร้างโปรเจคใหม่
3. เปิดใช้งาน **Firestore Database**
4. ตั้งค่า Security Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาตให้อ่านข้อมูลรอบรถได้ทุกคน
    match /trips/{tripId} {
      allow read: if true;
      allow write: if false; // เขียนได้เฉพาะแอดมิน (ตั้งค่าเพิ่มเติมตามต้องการ)
    }
    
    // อนุญาตให้สร้างการจองได้ทุกคน
    match /bookings/{bookingId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
```

### 2. คัดลอกไฟล์
1. คัดลอกไฟล์ทั้งหมดไปยัง web server หรือ Firebase Hosting
2. แก้ไข `js/firebase.js` ใส่ config ของคุณ (ถ้าจำเป็น)

### 3. รันโปรเจค
- เปิด `index.html` สำหรับหน้าจองคิว
- เปิด `admin.html` สำหรับหน้าเพิ่มรอบรถ

## 💾 โครงสร้าง Database

### Collection: `trips`
```javascript
{
  route: "กรุงเทพ → อุดรธานี",
  time: "08:00",
  seats: 10,          // ที่นั่งที่เหลือ
  price: 500,
  active: true,       // เปิด/ปิดการใช้งาน
  createdAt: "2025-12-16T10:30:00.000Z"
}
```

### Collection: `bookings`
```javascript
{
  tripId: "abc123",
  route: "กรุงเทพ → อุดรธานี",
  time: "08:00",
  customerName: "สมชาย ใจดี",
  customerPhone: "0812345678",
  seats: 2,           // จำนวนที่นั่งที่จอง
  totalPrice: 1000,
  bookingDate: "2025-12-16T11:00:00.000Z",
  status: "confirmed"
}
```

## 🆓 ค่าใช้จ่าย Firebase (Free Tier)
- **Firestore:** 50,000 reads/day, 20,000 writes/day
- **Storage:** 1GB
- **Hosting:** 10GB/month

เพียงพอสำหรับธุรกิจขนาดเล็ก-กลาง (ประมาณ 100-500 การจองต่อวัน)

## 🚀 คำแนะนำเพิ่มเติม

### การใช้งานจริง
1. **ตั้งค่า Authentication** - เพิ่มระบบล็อกอินสำหรับแอดมิน
2. **ปรับ Security Rules** - จำกัดสิทธิ์การเข้าถึงข้อมูล
3. **เพิ่มระบบแจ้งเตือน** - ส่ง SMS/Email ยืนยันการจอง
4. **เพิ่มระบบชำระเงิน** - เชื่อมต่อ Payment Gateway

### การพัฒนาต่อ
- [ ] หน้าดูรายการจองทั้งหมด (สำหรับแอดมิน)
- [ ] ระบบค้นหาการจองด้วยเบอร์โทร
- [ ] ระบบยกเลิกการจอง
- [ ] รายงานสถิติการจอง
- [ ] ระบบแจ้งเตือนทาง LINE

## 📱 การใช้งาน

### หน้าจองคิว (index.html)
1. เลือกรอบรถที่ต้องการ
2. กรอกชื่อและเบอร์โทรศัพท์
3. ระบุจำนวนที่นั่ง
4. กดปุ่ม "จองคิวเลย"
5. ระบบจะแสดงรายละเอียดการจอง

### หน้าแอดมิน (admin.html)
1. กรอกข้อมูลรอบรถ (เส้นทาง, เวลา, ที่นั่ง, ราคา)
2. เลือกเปิด/ปิดการใช้งาน
3. กดปุ่ม "บันทึกรอบรถ"
4. ดูรายการรอบรถด้านล่าง

## 🔒 Security

**⚠️ สำคัญ:** ไฟล์ `firebase.js` มี API Key ห้ามเผยแพร่ในที่สาธารณะ!

แนวทางแก้ไข:
1. ใช้ Environment Variables
2. ตั้งค่า Firebase Security Rules อย่างเข้มงวด
3. จำกัด API Key ใน Firebase Console

## 📞 ติดต่อ

ติดปัญหาหรือต้องการความช่วยเหลือ กรุณาติดต่อผู้พัฒนา

---

**พัฒนาด้วย ❤️ โดย Fei Yi Transporter Team**
